# REV2

> Source: `manuals/misc/DOC_2 - FLEX Software Archive - Documentation Disk - Includes ARERRS, CBUG, HEADERS.zip!DOC_2.DSK!REV2.PR`  
> Method: FLEX disk extraction

OCR and media-decoding errors may remain.

e inconsistency of the new-line being appended by puts and not by
fputs is dictated by history and the desire for compatibility.
.pg
qsort - quick sort
.us
    qsort(base,n,size,compfunc)
    char *base;
    int (*compfunc)(); /* which means:
         se functions are actually macros defined in 'stdio.h' so their
names cannot re-declared.
.se
    Low-level call open(),
    Fopen().
.pg
findstr,findnstr - string search
.us
    findstr(pos,string,pattern)
    char *string,*pattern;

    findnstr(pos,s there is no executable code.
To avoid this possibility, the following techniques are available:
.sp
.nf
        1.      Ensure that the program does not call any of the
                'dangerous' functions.
        2.      If you have the TSC librarystack pointer
        sts     _sttop          store as stack top
        sts     _stbot          and stack bottom
        ldd     \#-128           reserve some space
        jsr     _stkcheck

        jsr     _shell          get command-line args.
