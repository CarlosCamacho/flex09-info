# ALFADEMO

> Source: `dev/pascal/PASC6800 - Pascal Language - Program and Support Utilities.zip!PASC6800.DSK!ALFADEMO.TXT`  
> Method: FLEX disk extraction

The text below preserves the wording and formatter directives found in the historical source. OCR and media-decoding errors may remain.

PROGRAM ALFAS ; (* TO DEMONSTRATE THE INSERTION OF CHARACTERS *)
CONST MAX=100;	(* INTO AN ALFA VARIABLE, AND THEN ITS USE	*)
VAR
	POS,TOP : INTEGER ;
	NAME,BLANK : ALFA ;
	LIST : ARRAY [ 0..MAX ] OF ALFA ;

PROCEDURE GETNAME ;
	VAR
	J : INTEGER ; CH : CHAR ;
	BEGIN
	WRITE("ENTER A NAME - "); NAME:=BLANK ; J:=1;
	REPEAT
		READ(CH);
		IF CH = CHR ( 8 ) (* BACKSPACE *) THEN
		BEGIN

NAME[J]:=" "; J:=J-1;
		END ELSE
		BEGIN

IF J <= 6 THEN NAME[J]:=CH; J:=J+1
		END
	UNTIL EOLN ( INPUT ) ;
	END ;
	
FUNCTION POSITION ( IDENT : ALFA ) : INTEGER ;
	VAR
	LOW,MID,HIGH : INTEGER ;
	BEGIN	(* BINARY SEARCH *)
	LOW:=1; HIGH:=TOP;
	REPEAT
		MID:=(LOW+HIGH) DIV 2 ;
		IF IDENT <= LIST[MID] THEN HIGH:=MID-1;
		IF IDENT >= LIST[MID] THEN LOW:=MID+1;
	UNTIL LOW > HIGH ;
	IF LOW-1 > HIGH THEN POSITION:=MID ELSE POSITION:=0;
	END;

PROCEDURE ENTER ( IDENT : ALFA ) ;
	VAR
	MARK : INTEGER ;
	BEGIN
	LIST[0]:=BLANK; (* SENTINEL FOR WHILE LOOP *)
	MARK:=TOP; TOP:=TOP+1;
	IF TOP > MAX THEN
	BEGIN WRITE("TOO MANY ENTRIES"); HALT END ELSE
	WHILE LIST[MARK] > IDENT DO
	BEGIN
		LIST[MARK+1]:=LIST[MARK]; MARK:=MARK-1;
	END;
	LIST[MARK+1]:=IDENT ;
	WRITELN("NEW NAME	:- PUT IN POSITION",MARK+1);
	END;

BEGIN
	BLANK	:="	";LIST[1]:="ANN	"; LIST[2]:="BOB	";
	TOP:=2;
	REPEAT
	GETNAME ;
	IF NAME <> BLANK THEN
	BEGIN
		POS:=POSITION ( NAME ) ;
		IF POS = 0 THEN ENTER ( NAME ) ELSE
		WRITELN("	NAME ALREADY IN POSITION",POS);
	END
	UNTIL NAME = BLANK ;
END.
