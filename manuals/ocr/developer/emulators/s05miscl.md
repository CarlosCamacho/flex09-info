# S05MISCL

> Source: `dev/emulators/SIM68052 - Emulator Tools - Documentation and Examples.zip!SIM68052.DSK!S05MISCL.TXT`  
> Method: FLEX disk extraction

The text below preserves the wording and formatter directives found in the historical source. OCR and media-decoding errors may remain.

***************************************
**
HELPME JSR STOPIN HELP ROUTINE
 LDX #CLRSCN CLEAR SCREEN
 JSR OUTCUR
 LDX #HELPS
HELPYU STX BADDR
 JSR XCRLF
 LDX BADDR
 JSR XPDAT
 TST $00,X
 BNE HELPYU
 RTS
**
SETMOD JSR XCRLF SET CPU MODEL
 LDX #LMODE
 JSR XPDAT
SETMOE JSR XINEE
 ANDA #$5F
 CMPA #$20 SP
 BEQ SETMOE
 CMPA #$48 H
 BEQ SETMOH
 CMPA #$43 C
 BNE NEWBAD
SETMOH PSHA
 JSR XCRLF SET TIMER
 LDX #LTIME
 JSR XPDAT
 JSR XBADR
 STX SAVEW
 JSR XCRLF SET MAX MEMORY
 LDX #LMAXM
 JSR XPDAT
 JSR XBADR
 LDAA BADDR
 CMPA #$3F CHECK FOR 16K MAX
 BHI NEWBAD
 LDAA BADDR+1 CHK FOR $FF
 INCA
 BNE NEWBAD
 JSR XINEE
 PULA
 STAA PMODEL
 LDX BADDR
 STX MAXMEM
 LDX SAVEW
 STX PTIMER
 BNE SETMOX
 INC PTIMER+1
SETMOX RTS
**
NEWRNG LDX #INAM0 SET DIS RANGE
 JSR GADRS
 BLS NEWRG
NEWBAD JMP BADRG
NEWRG LDX INAM2
 STX TADDR
 LDX INAM0
 STX FADDR
 BNE SXFER CHECK FOR BAD RANGE
 LDX TADDR
 INX
 BNE SXFER
 LDX #$FFF0 FIX BAD UPPER BOUND
 STX TADDR
**
SXFER LDX #LXFER SET XFER ADDR
 JSR XPDAT
 JSR XBADR
 JSR XINEE
 STX XADDR
 STX PREGST
 RTS
**
OLOAD LDX #OFFIL SET LOAD OFFSET
 JSR XPDAT
 JSR XOUTS
 JSR XBADR
 STX OADDR
 RTS
**
FEXIT JSR XINEE EXIT
 JSR FMSCLO
 LDX #CLRSCN
 JSR OUTCUR
 LDX TTSDEP FIX DEPTH COUNT
 LDAA TTHDEP
 STAA $00,X
 LDX TTSWID FIX WIDTH COUNT
 LDAA TTHWID
 STAA $00,X
 LDX INCHRR FIX INCHR ADDR
 LDAA HINCHR
 STAA $01,X
 LDAA HINCHR+1
 STAA $02,X
 LDX OTCHRR FIX OUTCHR ADDR
 LDAA PRADDR
 STAA $01,X
 LDAA PRADDR+1
 STAA $02,X
 JMP WARMS EXIT
**
DOSCOM JSR STOPIN SEND MESSAGE TO FLEX
 LDAA #$2B PRINT +++
 JSR XOUTE
 JSR XOUTE
 JSR XOUTE
 LDAA #$03
 STAA ESCSWT SET ESCAPE SWITCH
 STS SAVES
 JSR INBUFR GET INPUT LINE
 LDX BUFPTR
 LDX $00,X
 LDAA $00,X
 CMPA #$0D
 BEQ DOSCOX SKIP IF NULL
 LDX TTSDEP FIX DEPTH COUNT
 LDAA TTHDEP
 STAA $00,X
 LDX TTSWID FIX WIDTH COUNT
 LDAA TTHWID
 STAA $00,X
 JSR DOXCOM CALL DOS
DOSCOY JSR FIXESC RESTORE ESCAPE
DOSCOX CLR ESCSWT
 RTS
