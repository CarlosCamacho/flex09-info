# CSSMISCL

> Source: `dev/disassemblers/SLEUTH - Disassembler Tools - Documentation and Examples.zip!SLEUTH.DSK!CSSMISCL.TXT`  
> Method: FLEX disk extraction

The text below preserves the wording and formatter directives found in the historical source. OCR and media-decoding errors may remain.

***************************************
**
HELPME JSR STOPIN HELP ROUTINE
 LDX #CLRSCN CLEAR SCREEN
 JSR OUTCUR
 LDX #HELPS
HELPYU STX BADDR
 JSR XCRLF
 LDX BADDR
 JSR XPDAT
 TST $00,X
 BNE HELPYU
 RTS
**
FCROF JSR XINEE FLIP CROSS-ASSEMBLER FLAG
 LDAA CROFL
 COMA
 STAA CROFL
 RTS
**
FEQUF JSR XINEE FLIP EQU FLAG
 LDAA EQUFL
 COMA
 STAA EQUFL
 RTS
**
FPOSF JSR XINEE FLIP POS-IND FLAG
 LDAA POSFL
 COMA
 STAA POSFL
 RTS
**
SETMOD JSR XCRLF SET CPU MODE
 LDX #LMODE
 JSR XPDAT
 JSR XNHEX
SETMOI LDAB #$07 MODE MASK
 TSTA ALLOW ONLY 0,1,2,3,5,8,9
 BNE SETMO0
SETMO1 LDX #INTAB0
 STX AINTAB
 LDX #OPTAB0
 STX AOPTAB
 LDX #SUFTB9
 STX ASUFTB
 ANDA #$01
 BRA SETMOX
SETMO0 CMPA #$01
 BEQ SETMO1
 CMPA #$02
 BNE SETMO2
 LDX #INTAB2
 STX AINTAB
 LDX #OPTAB2
 STX AOPTAB
 LDX #SUFTB5
 STX ASUFTB
 BRA SETMOM
SETMO2 CMPA #$03
 BEQ SETMO1
 CMPA #$05
 BNE SETMO5
 LDX #INTAB5
 STX AINTAB
 LDX #OPTAB5
 STX AOPTAB
 LDX #SUFTB5
 STX ASUFTB
 BRA SETMOM
SETMO5 CMPA #$08
 BEQ SETMO1
 CMPA #$09
 BNE SETMOD
 LDX #INTAB9
 STX AINTAB
 LDX #OPTAB9
 STX AOPTAB
 LDX #SUFTB9
 STX ASUFTB
 LDAA #$FF
 BRA SETMOX
SETMOM LDAB #$0F SET MASK
SETMOX STAA PMODE
 STAB MODMSK
 RTS
**
NEWRNG LDX #INAM0 SET DIS RANGE
 JSR GADRS
 BLS NEWRG
NEWRT JSR BADRG
 BRA NEWRNG
NEWRG LDX INAM2 NO END=$FFFF
 INX
 BEQ NEWRT
 DEX
 STX TADDR
 LDX INAM0
 STX FADDR
**
SXFER LDX #LXFER SET XFER ADDR
 JSR XPDAT
 JSR XBADR
 STX XADDR
 RTS
**
OLOAD LDX #OFFIL SET LOAD OFFSET
 JSR XPDAT
 JSR XOUTS
 JSR XBADR
 STX OADDR
 RTS
**
FEXIT JSR XINEE RESTART OR EXIT
 LDAA COMAND
 CMPA #$52 CHK R
 BNE FEXITS
 JMP START RESTART
FEXITS JSR FMSCLO
 LDX #CLRSCN
 JSR OUTCUR
 JMP WARMS
**
DOSCOM JSR STOPIN SEND MESSAGE TO FLEX
 LDAA #$2B PRINT +++
 JSR XOUTE
 JSR XOUTE
 JSR XOUTE
 LDAA #$03
 STAA ESCSWT SET ESCAPE SWITCH
 STS SAVES
 JSR INBUFR GET INPUT LINE
 LDX BUFPTR
 LDX $00,X
 LDAA $00,X
 CMPA #$0D
 BEQ DOSCOX SKIP IF NULL
 JSR DOXCOM CALL DOS
DOSCOY JSR FIXESC RESTORE ESCAPE
DOSCOX CLR ESCSWT
 RTS
