# CSSDMPTB

> Source: `dev/disassemblers/SLEUTH - Disassembler Tools - Documentation and Examples.zip!SLEUTH.DSK!CSSDMPTB.TXT`  
> Method: FLEX disk extraction

The text below preserves the wording and formatter directives found in the historical source. OCR and media-decoding errors may remain.

***************************************
**
DMPTAB JSR XCRLF DUMP TABLES
 LDX TYPEM
DMPTAC CPX TYPEE
 BNE DMPTAD
 JMP DMPTAE
DMPTAD STX CADDR PRINT A LINE
 JSR XCRLF
 LDX CADDR
 LDAA $00,X
 CMPA #$5A CHK Z
 BEQ DMPTA2
 JSR XOUTE TYPE
 LDAA #$20
 JSR XOUTE SPACE
 INX
 JSR XOT4S START
 JSR XOT4S END
 INX
DMPTA1 INX
 INX
 BRA DMPTAC
DMPTA2 LDAA $01,X CHK START/END
 CMPA $03,X
 BNE DMPTA4
 LDAA $02,X
 CMPA $04,X
 BNE DMPTA4
 LDAA #$4D MAKE M
 JSR XOUTE TYPE
 LDAA #$20
 JSR XOUTE SPACE
 INX
 INX
 INX
DMPTA3 JSR XOT4S END
 JSR XOT2S VALUE
 BRA DMPTA1
DMPTA4 LDAA #$54 MAKE T
 JSR XOUTE TYPE
 LDAA #$20
 JSR XOUTE SPACE
 INX
 JSR XOT4S START
 BRA DMPTA3
DMPTAE JSR XCRLF PRINT PARMS
 LDX FADDR
 INX
 BEQ DMPTAF
 LDX #LSTAR START=
 JSR XPDAT
 LDX #FADDR
 JSR XOT4H
 LDX #LENDR END=
 JSR XPDAT
 LDX #TADDR
 JSR XOT4H
DMPTAF LDX XADDR
 INX
 BEQ DMPTAG
 LDX #LXFER XFER=
 JSR XPDAT
 LDX #XADDR
 JSR XOT4H
DMPTAG JSR XCRLF
 LDX OADDR
 BEQ DMPTAH
 LDX #OFFIL OFFSET
 JSR XPDAT
 JSR XOUTS
 LDX #OADDR
 JSR XOT4H
 JSR XCRLF
DMPTAH LDX #EQUIL EQU FLAG
 JSR XPDAT
 LDX #LON
 TST EQUFL
 BNE DMPTAI
 LDX #LOFF
DMPTAI JSR XPDAT
 JSR XCRLF
 LDX #CPUIL CPU MODE
 JSR XPDAT
 JSR XOUTS
 LDAA #$36
 JSR XOUTE
 LDAB PMODE
 BPL DMPTAJ
 LDAB #$09
DMPTAJ ADDB #$30
 LDAA #$38
 CMPB #$32 CHK 2
 BNE DMPTAK
 LDAA #$35
DMPTAK JSR XOUTE
 LDAA #$30
 JSR XOUTE
 TBA
 JSR XOUTE
 JSR XCRLF
 LDX #POSIL POSITION FLAG
 JSR XPDAT
 LDX #LON
 TST POSFL
 BNE DMPTAL
 LDX #LOFF
DMPTAL JSR XPDAT
 JSR XCRLF
 LDX #CROIL CROSS-ASSEMBLER FLAG
 JSR XPDAT
 LDX #LON
 TST CROFL
 BNE DMPTAN
 LDX #LOFF
DMPTAN JSR XPDAT
 JSR XCRLF
 TST DISIF CHK FOR INPUT FILE
 BEQ DMPTAO
 LDX #DISIM
 JSR XPDAT
 LDX #FCBI+FCDRIV
 BSR DMPTAV
DMPTAO RTS
DMPTAV LDAA $00,X PRINT FILE INFO
 BPL DMPTAW
 LDAA #$01 ALL=1
DMPTAW ADDA #$30 DRIVE
 JSR XOUTE
 LDAA #FLSEP1 .
 JSR XOUTE
 LDAB #FCEXTN-FCNAME FILE NAME
DMPTAX INX
 LDAA $00,X
 JSR XOUTE
 DECB
 BNE DMPTAX
 LDAA #FLSEP2 .
 JSR XOUTE
 LDAB #$03 EXTENSION
DMPTAY INX
 LDAA $00,X
 JSR XOUTE
 DECB
 BNE DMPTAY
 RTS EXIT
