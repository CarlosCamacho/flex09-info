# S02MISCL

> Source: `dev/csc/CSCPRODS_4 - CSC Development - Programs, Source, and Build Files.zip!CSCPRODS_4.DSK!S02MISCL.TXT`  
> Method: FLEX disk extraction

The text below preserves the wording and formatter directives found in the historical source. OCR and media-decoding errors may remain.

ZAP SOURCE FILE (DANGER)Dangerous option selected Are you sure (Y/N):  NO    YES  BAD ENTRY:  DATE BAD:   ALLOW BAD DATES (Y/N):   to Drive #SINGLE COPY BY NUMBER NOT
@D ALLOWEDILLEGAL DIRECTORY NUMBERChange disk-press key:   File existsOND INPUT
STEROR LDB #$FF INVALID FUNCTION
STEXIT LDU MEMEND
 SEX
 STD -$04,U RETURN
 RTS
*
FUNS01 EQU * SET CURSOR
 LBSR SETCUR SET CURSOR
 BRA STEROR
*
FUNS02 EQU * OUTPUT STRING
 LBSR SETCUR SET CURSOR
 LEAX CONT30,PCR SET ATTRIBUTE
 LBSR SETATT
 LS14]6805, 6801, and 6502 simulators
are supplied in source form on disk - assembly is required.	The
systems are available on 5" or 8" diskette for FLEX* or OS/9* or UNIFLEX*.
They are priced and marketed separately; however, because they are
such similOPCDA0
OPCDA9 EQU * LDA IMM
 JMP OPCH98
OPCDAA EQU * TAX
 BRA OPCDA2
OPCDAB EQU *
 RTS
OPCDAC EQU * LDY EXT
 BRA OPCDA4
OPCDAD EQU * LDA EXT
 BRA OPCDA1
OPCDAE EQU * LDX EXT
 BRA OPCDA6
OPCDAF EQU *
 RTS
OPCDB0 EQU * BCS REL
 BITA #RC C=1
 BEQ OPCEB0

12960 IF CH$<="f" THEN POKE DPEEK(PTR(QS$))+I%,ASC(CH$)-32\GOTO 12900
12970 GOTO 12850
12980 NL$=LEFT$(LN$,12)+QS$+MID$(LN$,17)\LN$=NL$\GOTO 10420
12990 REM VIDEO BLINK DELAY CONSTANT
13000 PRINT "Enter video board blink delay constant (Was: ";
13010 be
possible and advantageous to code a set of 6809 routines to
interface to a similar-functional library or routines.
Then this interface may be used in any program with few
other changes in logic required.
.sp
I/O address and hardware differences may px File couldn't be deleted.'=T''
~
.  File couldn't be created.6C*7Dj6C27Dr|C4}Dt6C67Dv7Ds@'ejF}@.  File couldn't be copied..  Disk will belinked to this file.  Disk couldn't be linked


&1[H@'Lm
	'=T&
