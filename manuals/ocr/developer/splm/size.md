# SIZE

> Source: `dev/splm/SPLM2 - SPLM Language - Program and Support Utilities.zip!SPLM2.DSK!SIZE.TXT`  
> Method: FLEX disk extraction

The text below preserves the wording and formatter directives found in the historical source. OCR and media-decoding errors may remain.

/* SIZE -- DISPLAYS SECTOR COUNT,	*/
/* LENGTH IN DECIMAL AND HEX,	*/
/* NUMBER OF LINES (CR'S), PLUS	*/
/* CHECKSUM AND CREATION DATE OF	*/
/* A FILE.	*/
/*	!*/
/*	FLEX VERSION 1.0		*/
/*
6-11-79	*/

0A100H:;
DCL VERSION LIT '1';

0A840H:DCL RFCB (320) BYTE;

/* #INCLUDE SPLM.LIB	-- LIBRARIES INCLUDED HERE
	#INCLUDE SPLMREAD.LIB	*/
#INCLUDE SPLM.LIB
#INCLUDE SPLMREAD.LIB

DATE:PROC;	/* OUTPUT DATE AS	MM-DD-YY	*/
	DCL MONTH LIT '25', DAY LIT '26', YEAR LIT '27';
	DCL DGT ADDR;
	LDSPC = FALSE;
	IF RFCB(MONTH) < 10 THEN CALL SPACE;
	DGTA = .DGT;
	DGT = RFCB(MONTH); CALL OUTDEC;
	CHAR = '-'; CALL PUTCHR;
	DGT = RFCB(DAY); CALL OUTDEC;
	CHAR = '-'; CALL PUTCHR;
	DGT = RFCB(YEAR); CALL OUTDEC;
	IF RFCB(DAY) < 10 THEN CALL SPACE;
	CALL SPACE;
END;

ASIZE:PROC;	/* OUTPUT SIZE AND CHECKSUM INFO FOR A FILE */
	DCL BYTE$CTR ADDR, LINE$CTR ADDR, CHKSUM BYTE;
	DCL TBYTE$CTR ADDR, FLAG BYTE;
	DCL XSIZ LIT '21'; /* LOC OF SECTOR SIZE IN FCB */
	DCL CR LIT '0DH';

	BYTE$CTR = 0; LINE$CTR = 0; FLAG = FALSE; CHKSUM = 0;
	CALL RBFD;
	DO WHILE NOT REOF;
	IF FLAG AND (CHAR <> 0) THEN FLAG = FALSE;
	IF NOT FLAG AND (CHAR = 0) THEN DO;
	FLAG = TRUE;
	/* MARK LAST NON-ZERO BYTE */
	TBYTE$CTR = BYTE$CTR;
	END;
	CHKSUM = CHKSUM + CHAR;
	BYTE$CTR = BYTE$CTR + 1;
	IF CHAR = CR THEN LINE$CTR = LINE$CTR + 1;
	CALL RBFD;
	END;
	IF FLAG THEN	/* STRING OF NULLS AT END */
	BYTE$CTR = TBYTE$CTR;
	
	LDSPC = TRUE;
	DGTA = .RFCB+XSIZ; CALL OUTDEC; /* SECTOR SIZE */
	CALL SPACE;

	DGTA = .BYTE$CTR; CALL OUTDEC;	/* BYTE COUNT */
	CALL SPACE; CALL SPACE;

	CALL OUTADR;	/* IN HEX */
	CALL SPACE;

	DGTA = .LINE$CTR; CALL OUTDEC;	/* LINE COUNT */
	CALL SPACE; CALL SPACE;

	DGTA = .CHKSUM; CALL OUTHEX;	/* CHECKSUM */
END;

/* MAIN */
DCL HEADER DATA ('	DATE	NS	DEC	HEX LINES	CS',
	CRLF,CRLF,4);

DEFEXT = DXTXT;
CALL RDOPEN;

MSGA = .HEADER; CALL PSTRNG;
CALL DATE;
CALL ASIZE;

CALL RDCLOSE;
CALL WARMS;

EOF
