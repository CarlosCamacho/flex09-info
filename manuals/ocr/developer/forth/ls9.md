# LS9

> Source: `dev/forth/CC_FORTH - FORTH Language - Program and Support Utilities.zip!CC_FORTH.DSK!LS9.TXT`  
> Method: FLEX disk extraction

The text below preserves the wording and formatter directives found in the historical source. OCR and media-decoding errors may remain.

TTL	FLEX PUNCH & LOAD S9 RECORDS
*
PORTAD EQU	$E060	PORT ADDRESS OF RECEIVED DATA
FLEXPT EQU	$E005	FLEX I/O PORT
WARMS	EQU	$CD03
*
	ORG	$C100
*
LOADT	JSR	INITPT
LOAD	JSR	INEEE
	CMPA	#'S
	BNE	LOAD	MUST BE 'S' TO START READ
	JSR	INEEE	READ CHARACTER FROM TAPE
	CMPA	#'9
	BEQ	LOAD21	END OF FILE, EXIT
	CMPA	#'1
	BNE	LOAD	READ 65K
DOLOAD JSR	BYTE	READ BYTE OF DATA
	SUBA	#2
	PSHS	A		NUMBER OF BYTES READ
DOLD1	JSR	BADDR	READ LOAD ADDRESS
LOAD11 JSR	BYTE	READ DATA BYTES
	DEC	S
	BEQ	LOAD	ZERO OUT BYTE COUNT
	STA	X		STORE DATA IN MEMORY
	CMPA	,X+	READ TO VERIFY STORAGE
	BEQ	LOAD11	IF NOT, PRINT ? AND EXIT
	LDA	#'?	MIS-READ
	JSR	OUTEEE	TELL THE OPERATOR
LOAD21 JMP	WARMS	ONLY EXIT FROM ROUTINE
*
INITPT LDY	#PORTAD
	LDD	#$0315
	STA	Y
INIT	INCA
	BNE	INIT
	STB	Y
	RTS
*
BADDR	BSR	BYTE
	PSHS	A
	BSR	BYTE
	PULS	B
	EXG	A,B
	TFR	D,X
	RTS
*
BYTE	BSR	INHEX
	ASLA
	ASLA
	ASLA
	ASLA
	PSHS	A
	BSR	INHEX
	ORA	,S+
	RTS
*
INHEX	JSR	INEEE
	SUBA	#'0
	CMPA	#9
	BLS	INH
	SUBA	#7
INH	RTS
*
INEEE	LDY	#PORTAD
	PSHS	B
INEE1	LDB	,Y
	BITB	#1
	BEQ	INEE1
	LDA	1,Y
	ANDA	#$7F
	STA	FLEXPT
	PULS	B,PC
*
OUTEEE LDY	#FLEXPT-1
	PSHS	B
OUTEE1 LDB	Y
	BITB	#2
	BEQ	OUTEE1
	STA	1,Y
	PULS	B,PC
*
	END	LOADT
